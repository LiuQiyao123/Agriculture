# 智能农业管理平台 - 需求规格说明书

**版本**: v2.4 (优化与澄清版)
**日期**: 2025-09-19

---

## 1. 总览

本文档旨在详细定义智能农业管理平台的功能需求、交互流程和设计规范。系统基于最新的模块划分方案，构建一个以AI为核心驱动，集“分析决策 → 农事管理 → 资源调度”于一体的业务闭环。**本文档可直接交付UI/UX设计师、前端和后端开发团队使用。**

### 1.1. 核心模块清单
- **1. 农业态势大屏** (全局概览)
- **2. 智能分析决策** (AI大脑)
- **3. 智慧农事管理** (生产执行)
- **4. 农资管理** (后勤保障)
- **5. 数据服务中心** (平台支撑)

---

## 2. 通用组件库 (Component Library)

本章节定义了系统内可被多个页面复用的标准化UI组件，以确保视觉和交互的一致性。

| 组件名称 | 预览/描述 | 核心功能与交互 |
| --- | --- | --- |
| **`GisMap`** | GIS地图组件 | - **功能**: 渲染GIS矢量地块、设备点位、热力图等。<br>- **交互**: 支持缩放、平移、点击/悬浮查询、边界绘制。 |
| **`StatisticCard`** | 大型数字统计卡片 | - **功能**: 展示单个核心KPI，包含标题、数值、单位和环比。<br>- **交互**: 支持点击跳转到详情页。 |
| **`ScrollingList`** | 垂直无缝滚动列表 | - **功能**: 用于展示实时更新的列表，如预警通知。<br>- **交互**: 支持点击列表项进行响应。 |
| **`KanbanBoard`** | 看板组件 | - **功能**: 以列和卡片的形式可视化任务流。<br>- **交互**: 支持卡片在列之间拖拽，卡片点击查看详情。 |
| **`GanttChart`** | 甘特图/时间轴组件 | - **功能**: 可视化展示任务或资源在时间维度上的排布。<br>- **交互**: 支持时间段的拖拽、拉伸，查看任务依赖。 |
| **`DataTable`** | 数据表格组件 | - **功能**: 展示结构化数据，支持排序、筛选、分页。<br>- **交互**: 支持行点击、行选择、单元格自定义渲染。 |
| **`Modal`** | 模态框/弹窗组件 | - **功能**: 在当前页面之上打开一个浮层，用于展示详情或执行表单操作。<br>- **交互**: 支持确认、取消、关闭操作。 |
| **`(新增) DashboardChartCard`** | 仪表板图表卡片 | - **功能**: 用于`数据统计分析`页面的可拖拽、可配置的图表容器。<br>- **交互**: 支持配置图表类型、指标和维度。 |
| **`(新增) RecommendationCard`** | AI推荐方案卡片 | - **功能**: 用于在`种植规划`、`采收任务`中，标准化地展示和对比AI推荐的方案。<br>- **交互**: 支持方案选择、查看详情。 |

---

## 3. 页面详细设计

### **模块一：农业态势大屏**

**定位**: 为农场管理者提供一站式的全局态势感知中心，实现“一张图看全局”。

---

#### **页面: 驾驶舱主页 (`/dashboard`)**

*   **用户故事**: 作为农场管理者，我希望能在一个页面上快速了解整个农场的宏观状态，以便我能及时发现问题，做出宏观决策。

*   **页面布局**: 采用“品”字形布局。
    *   **顶部 (大尺寸)**: **农业一张图**，作为页面的视觉和信息核心。
    *   **左下**: **核心KPI指标卡组** + **农事任务概览**。
    *   **右下**: **关键预警通知列表** + **近期天气提醒**。

*   **核心组件引用**:
    1.  **农业一张图**: 使用 `GisMap` 组件。**核心是加载一个“地块健康评分”图层**，地块根据AI健康评分进行实时颜色渲染（绿/黄/红）。
    2.  **核心KPI指标**: 使用一组6个 `StatisticCard` 组件，展示地块、生产、成本等核心指标。
    3.  **关键预警通知**: 使用 `ScrollingList` 组件，实时滚动`农事预警系统`推送的高危预警。
    4.  **农事任务概览**: 使用 `ECharts` 环形图组件，展示任务状态占比。

*   **后端API需求**: `GET /api/dashboard/summary` (获取所有聚合数据)。

---

### **模块二：智能分析决策**

**定位**: 系统的“AI大脑”，**此模块的功能和UI/UX维持现有成功设计，不做大的改动**。

#### **内部子模块清单 (最终修正版)**
遵照您的指示，已对所有子模块统一命名范式，并移除所有多余的层级分类。**后续将以此处清单为基础，为每个模块提供“施工图纸”级别的详细设计。**

1.  **农业气象预警 (新增)**
2.  **地块利用评估**
3.  **地力退化评估**
4.  **土壤健康诊断**
5.  **土壤墒情监测**
6.  **作物长势诊断**
7.  **作物产量预测**
8.  **病虫害风险预警**

| 功能点 | 详细需求 |
| --- | --- |
| **(维持现状)** | - 所有分析子模块均维持其现有UI和交互流程。 |
| **(新增) 决策输出接口** | - **后端需求**: 为每个分析模块开发标准化的决策输出API，供`智慧农事管理`模块调用。例如，`GET /api/analysis/crop_growth/recommendations`。 |

---

## 4. “施工图纸”级详细设计

---

### **模块 2.1: 农业气象预警**

*   **用户故事**: 作为生产主管，我需要在一个页面上集中查看所有关键气象数据及其变化趋势，并能接收到灾害性天气的预警，以便我能提前安排农事活动，规避风险。

---

#### **4.1.1. 页面布局与UI元素详解**

*   **整体布局**: 采用上、中、下三部分经典仪表盘布局。
    *   **上**: 核心指标卡组
    *   **中**: 地图与24小时天气预报
    *   **下**: 详细数据图表

*   **UI元素详解**:

    1.  **核心指标卡组 (使用 `StatisticCard` 组件)**
        *   **内容**: 横向排列4个卡片，分别显示：
            *   **实时温度**: `26℃`，下方小字 `体感 28℃`，附带昨日同比变化箭头 `↑ 1℃`。
            *   **实时湿度**: `75%`，下方小字 `中等`，附带昨日同比 `↓ 5%`。
            *   **风速与风向**: `12 km/h`，下方小字 `东南风`。
            *   **今日降雨量**: `5 mm`，下方小-字 `小雨`。
        *   **样式**: 数字字体巨大突出，正向变化（如温度升高）用暖色调（橙色），负向变化用冷色调（蓝色）。

    2.  **GIS地图 (使用 `GisMap` 组件)**
        *   **内容**:
            *   默认展示农场范围，地图上清晰标注出各个**气象监测站**的设备点位图标。
            *   地图右上角有一个图层切换控件，可以选择叠加**“温度热力图”、“湿度热力图”、“风速等值线图”**。
        *   **样式**: 气象站图标默认蓝色，若设备离线则变为灰色，若有告警（如电量低）则闪烁黄色。

    3.  **未来24小时天气预报**
        *   **内容**: 在地图下方，一个水平滚动的时间轴，以每3小时为一个节点，显示未来24小时的天气图标、温度和降雨概率。
        *   **样式**: 滚动条样式优化，节点之间有虚线连接，当前时间节点高亮显示。

    4.  **数据图表区 (使用 `DashboardChartCard` 组件)**
        *   **内容**: 默认并排展示两个图表卡片：
            *   **左图**: “过去7日温/湿度变化”，使用 `ECharts` 双Y轴折线图，一条线表示温度，一条线表示湿度。
            *   **右图**: “过去7日降雨量”，使用 `ECharts` 柱状图。
        *   **样式**: 每个卡片都有独立的标题和时间范围选择器。图表配色遵循系统整体UI规范。

---

#### **4.1.2. 核心交互逻辑**

1.  **查看气象站详情**:
    *   **触发**: 用户点击GIS地图上的任一`气象监测站`图标。
    *   **响应**: 弹出一个`Modal`模态框。
    *   **界面**: `Modal`内部分为左右两栏。左侧显示该站点的实时数据列表。右侧显示该站点过去24小时的温度、湿度、光照强度三条曲线的 `ECharts` 图表。

2.  **切换地图图层**:
    *   **触发**: 用户点击地图右上角的图层切换控件。
    *   **响应**: 弹出一个下拉菜单，包含“温度热力图”等选项。
    *   **界面**: 用户选择一项后，地图上平滑地叠加或切换显示相应的可视化图层。

3.  **接收灾害预警**:
    *   **触发**: 后端推送灾害预警信息（如“未来2小时内可能发生强降雨”）。
    *   **响应**: 页面顶部弹出一个醒目的、带背景色的横幅通知（Alert Bar）。
    *   **界面**: 通知内容包含预警类型、时间和建议措施，并有一个“知道了”按钮可关闭通知。该通知同时会推送到`农业态势大屏`的滚动预警列表中。

---

#### **4.1.3. 后端API需求 (含Payload)**

1.  **获取页面聚合数据**
    *   **Endpoint**: `GET /api/analysis/weather/summary`
    *   **Response Payload (示例)**:
        ```json
        {
          "kpi": {
            "temperature": { "value": 26, "feel": 28, "change": 1 },
            "humidity": { "value": 75, "change": -5 },
            "wind": { "speed": 12, "direction": "东南风" },
            "rainfall": { "value": 5 }
          },
          "stations": [
            { "id": "station-01", "name": "1号站", "coords": [118.0, 36.0], "status": "online" }
          ],
          "forecast_24h": [
            { "time": "14:00", "icon": "cloudy", "temp": 27, "rain_prob": 0.1 },
            { "time": "17:00", "icon": "rainy", "temp": 25, "rain_prob": 0.8 }
          ]
        }
        ```

2.  **获取图表数据**
    *   **Endpoint**: `GET /api/analysis/weather/charts?range=7d`
    *   **Response Payload (示例)**:
        ```json
        {
          "temperature": [{ "date": "2025-09-18", "value": 25 }],
          "humidity": [{ "date": "2025-09-18", "value": 80 }],
          "rainfall": [{ "date": "2025-09-18", "value": 2 }]
        }
        ```

3.  **获取单个气象站详情**
    *   **Endpoint**: `GET /api/analysis/weather/stations/{id}`
    *   **Response Payload (示例)**:
        ```json
        {
          "realtime_data": { "temp": 26, "humidity": 75, "light": 8000 },
          "history_24h": {
            "temperature": [{ "time": "13:00", "value": 26 }],
            "humidity": [{ "time": "13:00", "value": 75 }]
          }
        }
        ```

---

### **模块三：智慧农事管理**

**定位**: 系统的生产执行核心。将AI建议转化为可管理的农事活动。

---

#### **页面: 地块管理 (`/farm/plots`)**

*   **用户故事**: 作为农场主，我需要一个工具来管理我所有的地块，包括在地图上直观地绘制、新增地块，并能随时查看每个地块关联的所有信息。
*   **功能定位说明**: **本模块提供对地块的深度管理功能，是对`农业态势大屏`中地块概览的补充和深化。大屏用于“看”，本模块用于“管”。**

*   **页面布局**:
    *   **顶部**: 操作栏，包含“新增地块”按钮和搜索框。
    *   **主区域**: 左右分栏布局。
        *   **左侧**: 使用 `DataTable` 组件，展示地块列表。
        *   **右侧**: 使用 `GisMap` 组件，同步高亮展示左侧选中的地块。

*   **核心交互**:
    1.  **新增地块**: 点击“新增地块”，打开一个全屏 `Modal`。
        *   **表单**: 填写地块名称、编码等信息。
        *   **地图绘制**: 用户可在 `GisMap` 上使用绘图工具（多边形）直接绘制地块边界，系统自动计算面积。
        *   **保存**: 提交后，新地块出现在列表和地图中。
    2.  **地块详情**: 点击 `DataTable` 中的任一地块，进入其详情页 (`/farm/plots/{id}`)。详情页聚合展示与该地块相关的所有信息：基本信息、当前种植计划、历史种植记录、进行中的农事任务等。

*   **后端API需求**:
    *   `GET /api/plots`: 获取地块列表（含GIS边界数据）。
    *   `POST /api/plots`: 创建新地块。
    *   `GET /api/plots/{id}`: 获取单个地块的详细聚合信息。

---

#### **页面: 种植规划 (`/farm/plans`)**

*   **用户故事**: 作为农场规划者，我希望能基于AI的建议来制定科学的年度种植计划，而不仅仅是依赖过往经验，以实现利润最大化。

*   **页面布局**:
    *   **顶部**: “创建新种植计划”按钮。
    *   **主区域**: 使用 `DataTable` 展示所有种植计划，包含作物、关联地块、时间周期、当前状态（计划中/进行中/已完成）等列。

*   **核心交互 (AI种植决策)**:
    1.  **启动向导**: 点击“创建新种植计划”，打开一个分步 `Modal`。
    2.  **选择地块与设定目标**: 用户选择一个或多个地块，并设定种植目标（如“利润最大化”、“产量最高”或“最省水”）。
    3.  **获取AI建议**: 系统调用后端AI模型，分析市场行情、天气预测、地块土壤数据后，返回一个推荐方案列表。每个方案包含推荐作物、预估产量、预估利润、风险评级。
    4.  **人工校对与生成**: 用户查看方案对比，选择一个并可进行微调（如更换品种）。确认后，系统生成完整的种植计划，并自动创建关联的种植档案。

*   **后端API需求**:
    *   `POST /api/plans/recommendations`: 用户提交地块和目标，获取AI种植推荐方案。
    *   `POST /api/plans`: 创建一个新的种植计划。
    *   `GET /api/plans`: 获取所有种植计划的列表。

---

#### **页面: 任务看板 (`/farm/tasks`)**

*   **用户故事**: 作为生产主管，我需要一个任务看板来可视化地管理所有农事任务，跟踪它们的进度，确保没有任务被遗漏或延期。

*   **页面布局**:
    *   **顶部**: 筛选器（按负责人、地块等）。
    *   **主区域**: 使用 `KanbanBoard` 组件，包含“待处理”、“进行中”、“待审核”、“已完成”四列。

*   **核心交互 (AI任务自动生成)**:
    1.  **AI任务来源**: 后台服务轮询`智能分析决策`模块的API，自动在“待处理”列创建任务卡片。
    2.  **任务处理**: 主管在“待处理”列看到AI生成的任务，点击打开`Modal`，使用`Form`指派负责人。
    3.  **状态流转**: 任务卡片在看板列之间拖拽，调用 `PUT /api/tasks/{id}/status` 接口更新状态。

*   **后端API需求**:
    *   `GET /api/tasks`: 获取所有任务数据。
    *   `PUT /api/tasks/{id}`: 更新任务（如分配负责人）。

---

#### **页面: 采收任务管理 (`/farm/harvesting`)**

*   **用户故事**: 作为农场管理者，我希望能借助AI来决定最佳的采收时机，而不仅仅是看天气和经验，以确保作物在品质和产量上都达到峰值。

*   **页面布局**:
    *   **顶部**: “创建采收计划”按钮。
    *   **主区域**: 使用 `DataTable` 展示所有采收计划/任务，包含作物、关联地块、预计采收期、状态（计划中/进行中/已完成）。

*   **核心交互 (AI采收决策)**:
    1.  **启动决策**: 用户点击“创建采收计划”，选择一个处于成熟期的种植地块。
    2.  **获取AI建议**: 系统调用后端AI模型，综合分析作物品种的成熟度曲线、未来的短期天气预报、`智能分析决策`模块的作物长势数据以及市场价格趋势。
    3.  **方案呈现**: AI返回一个或多个推荐采收方案，每个方案包含：
        *   **最佳采收窗口期** (例如, 9月25日 - 9月28日)。
        *   **预估产量与品质等级**。
        *   **采收策略建议** (例如, “分批采收，优先采摘南侧区域”)。
    4.  **确认与任务生成**: 用户选择一个方案并确认后，系统自动创建一个采收计划，并可在`任务看板`中分解为具体的采收执行任务。

*   **后端API需求**:
    *   `POST /api/harvesting/recommendations`: 用户提交地块ID，获取AI采收推荐方案。
    *   `POST /api/harvesting/plans`: 根据选定的方案创建采收计划。
    *   `GET /api/harvesting/plans`: 获取所有采收计划列表。

---

#### **页面: 农事预警中心 (`/farm/alerts`)**

*   **用户故事**: 作为生产主管，我需要一个地方能看到所有的历史预警，以便我复盘问题，看看哪些地块或者哪些作物总是出状况，从而进行针对性改进。

*   **页面布局**:
    *   **顶部**: 强大的筛选器，可按预警类型（病虫害、干旱、设备离线）、严重等级、地块、时间范围进行筛选。
    *   **主区域**: 使用 `DataTable` 展示预警列表，包含预警内容、来源、时间和处理状态（待处理/已关闭）。

*   **核心交互**:
    1.  **查看详情**: 点击任一预警，打开`Modal`，展示预警详情。
        *   **数据快照**: 显示触发预警时的相关数据图表（例如，土壤湿度曲线，或作物健康雷达图的快照）。
        *   **处理记录**: 可记录处理过程和结果，形成问题知识库。
    2.  **状态管理**: 主管可以在此页面批量或单独“确认”或“关闭”预警。

*   **后端API需求**:
    *   `GET /api/alerts`: 获取预警列表（支持所有筛选参数）。
    *   `GET /api/alerts/{id}`: 获取单个预警的详细信息及数据快照。
    *   `PUT /api/alerts/{id}/status`: 更新预警的处理状态。

---

#### **页面: 数据统计分析 (`/farm/statistics`)**

*   **用户故事**: 作为农场主，我需要用数据说话，清晰地看到农场的投入产出比、各项成本以及生产效率，以支持我做下一年度的预算和规划。

*   **页面布局**: 仪表板式自由布局。
    *   **顶部**: 时间范围选择器和全局筛选器（按地块、作物品种）。
    *   **主区域**: 由多个可自定义的`数据分析卡片`组成，用户可以自由添加、移除和拖拽布局。

*   **核心组件与交互**:
    1.  **预置报表**: 系统提供多个预置的`数据分析卡片`模板，例如：
        *   **产量分析**: 使用 `ECharts` 条形图，对比不同地块或作物的单位面积产量。
        *   **成本分析**: 使用 `ECharts` 饼图，分析农资、人工、机械等成本构成。
        *   **任务效率分析**: 使用 `DataTable`，统计各类任务的平均完成时长和延期率。
        *   **投入产出比 (ROI) 分析**: 结合成本和产量数据，计算并展示每个种植计划的ROI。
    2.  **自定义报表**: 用户可以点击“添加图表”，选择图表类型（折线图、柱状图等），并自主选择`指标`（如：产量）和`维度`（如：按地块），创建个性化的分析图表。

*   **后端API需求**:
    *   这是一个典型的OLAP（在线分析处理）场景，需要一个灵活的聚合查询接口。
    *   `POST /api/statistics/query`:
        *   **Request Body**: `{ "metrics": ["yield", "cost"], "dimensions": ["plot", "crop"], "timeRange": ["start", "end"] }`
        *   **Response**: 返回聚合后的结构化数据，供前端渲染图表。

---

### **模块四：农资管理**

**定位**: 农场的“后勤保障部”，负责农资、农机、人员等核心资源的统一管理和智能调度。

---

#### **页面: 农机调度 (`/resources/machinery`)**

*   **用户故事**: 作为调度员，我需要知道所有农机的当前状态和排班，以便在接到任务时，能快速、高效地分配合适的农机。

*   **页面布局**:
    *   **左侧**: 使用 `DataTable` 组件展示农机列表，包含状态列。
    *   **右侧**: 使用 `GanttChart` 组件，可视化展示农机在未来一周的排班。

*   **核心交互 (智能调度)**:
    1.  从农事任务详情页点击“调度农机”进入此页面。
    2.  AI自动在左侧`DataTable`中高亮推荐最合适的农机。
    3.  调度员在右侧`GanttChart`的空闲时间段上拖拽，为该任务创建排班，打开`Modal`确认。

*   **后端API需求**:
    *   `GET /api/machinery/schedule`: 获取农机排班数据。
    *   `POST /api/machinery/schedule`: 创建新的农机排班。

---

#### **页面: 农资库存管理 (`/resources/supplies`)**

*   **用户故事**: 作为仓库管理员，我需要实时查看所有农资的库存，管理出入库记录，并接收低库存预警，以保障农事活动的物资供应。

*   **页面布局**:
    *   **顶部**: 使用一组 `StatisticCard` 组件，展示“农资种类总数”、“库存总价值”、“低库存预警数”。
    *   **主区域**: 使用 `DataTable` 组件展示农资列表，包含实时库存、预警线、状态等列。
    *   **操作栏**: “新增农资”、“入库”、“出库”按钮。

*   **核心交互**:
    1.  **出入库**: 点击“入库”或“出库”，打开`Modal`，使用`Form`填写数量、关联任务（出库时）、供应商等信息。提交后，`DataTable`中的库存实时更新。
    2.  **低库存预警**: 当`DataTable`中某行物资的库存低于预警线时，该行背景高亮为黄色或红色，并自动在驾驶舱的`ScrollingList`中创建一条预警通知。

*   **后端API需求**:
    *   `GET /api/supplies`: 获取所有农资及其库存列表。
    *   `POST /api/supplies/stock-in`: 添加入库记录。
    *   `POST /api/supplies/stock-out`: 添加出库记录，需关联任务ID。

---

#### **页面: 人员管理与调度 (`/resources/staff`)**

*   **用户故事**: 作为人事或生产主管，我需要管理所有员工的信息，并能根据任务需求，快速找到并指派具备相应技能的空闲员工。

*   **页面布局**:
    *   **顶部**: 筛选器（按技能、部门、空闲状态）。
    *   **主区域**: 使用 `DataTable` 组件展示员工列表，包含姓名、部门、主要技能（标签形式）、当前状态（空闲/任务中）等列。

*   **核心交互**:
    1.  **人员信息管理**: 支持对员工信息的增、删、改、查。点击员工姓名可进入详情页，查看其详细资料、技能证书、历史任务记录。
    2.  **智能指派 (来自任务)**: 在`任务看板`分配负责人时，系统可弹出一个`Modal`，调用此模块的API，**智能推荐**最合适的员工人选列表（基于技能匹配度、历史表现、当前是否空闲）。
    3.  **手动排班**: 主管也可以在此页面直接为员工指派非任务类的排班（如“培训”、“值班”），相关信息会同步到员工的个人日历中。

*   **后端API需求**:
    *   `GET /api/staff`: 获取所有员工列表。
    *   `GET /api/staff/recommendations?taskId={id}`: 为特定任务获取推荐的员工列表。
    *   `POST /api/staff/{id}/schedule`: 为员工创建新的排班项。

---

### **模块五：数据服务中心**

**定位**: 系统的平台级支撑，**大部分为后端功能，或简单的表格式管理后台**。
- **(维持现状)** 提供标准的用户、角色、权限管理等CRUD界面。
